apiVersion: v1
kind: Service
metadata:
  name: {{ include "test-nginx.fullname" . }}
  labels:
    {{- include "test-nginx.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "test-nginx.labels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
